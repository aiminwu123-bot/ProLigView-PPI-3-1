# ProLigView 使用说明（简要版）

**ProLigView — 蛋白互作三维结构可视化与评估工具**

在线地址：https://aiminwu123-bot.github.io/ProLigView-PPI-3-1/

- 纯网页工具：无需安装、无需注册、无需 GitHub 账号，浏览器打开即用（推荐 Chrome / Edge）
- 隐私安全：结构文件只在你自己的浏览器本地解析，**不会上传到任何服务器**

---

## 支持的文件格式

- PDB（.pdb）、mmCIF（.cif），直接支持 AlphaFold3 / AlphaFold Server 输出的 CIF 模型
- 载入方式：把文件**拖进页面**，或点击选择文件

---

## 模块一：PPI 分析（蛋白-蛋白互作）

**用途**：分析一个复合物结构内两条（或多条）链之间的相互作用。

1. 载入一个含多条链的复合物结构（PDB / CIF）
2. 在「链选择与 PPI 设定」中**点击链名**，把链分为受体（黄）和配体（紫）两组
3. 工具自动检测 8 类互作：氢键、盐桥、疏水、π-π、π-阳离子、二硫键、卤键、范德华
4. 查看结果：
   - 互作统计卡片 + 详细互作列表（**点击任意一条记录，视角自动定位到该位点**）
   - **界面残基清单**（统计表下方，点击任一残基自动定位；粗框=核心 pLDDT>70，暗淡=被 pLDDT 过滤未计入）
   - 界面残基统计（原始 / 有效 / 核心三级）与 BSA 埋藏面积
   - 自动生成的综合分析结论（高可信度 / 中等偏弱 / 不足 三档判定）
5. 导出：**分析报告（TXT，含全部互作统计与接触对明细）**、结构截图（PNG）

常用选项：
- **界面距离**：判定界面残基的距离阈值，默认 5.0 Å，可调
- **pLDDT 过滤**：AlphaFold 结构自动识别并启用（滤除低置信度区域），实验结构自动跳过
- **互作虚线显示**：8 类互作的虚线可按类型单独开关（只影响显示，不影响统计）
- **界面聚焦**：淡化非界面部分，突出显示互作界面与氢键等互作虚线
- **界面 / 非界面 Cartoon 透明度**：两个独立滑块，可分别精调界面残基与其余部分的显示浓度；非界面滑块始终生效（不依赖聚焦开关），聚焦开启时界面残基按界面滑块高亮
- **显示分子表面**：叠加半透明分子表面，透明度可调；默认**表面避开界面残基（开窗）**——界面区域的卡通、侧链与互作虚线从表面窗口中清晰露出，可关闭
- **互作链拉开距离**：把受体/配体沿质心连线拉开 0–30 Å（"爆炸视图"），方便观察埋藏在界面内部的残基；互作虚线保持连接两端，统计数据不受影响
- **导出 PNG**：固定 3 倍分辨率输出，图片清晰可直接用于论文/汇报

> 说明：3D 视图中高亮的是**全部界面残基**（链间 ≤5Å），互作统计表列出的是其中的**特异性接触对**（氢键/盐桥等），两者数量本就不同——界面残基清单可在左侧面板逐一查看定位。

> 评判口径：界面残基按残基级距离判定；氢键/盐桥等按残基对去重计数；AF3 结构按 pLDDT>50（有效）/ >70（核心）过滤；BSA 为 Shrake-Rupley 算法估算的埋藏表面积。

---

## 模块二：结构叠加（WT vs Mut）

**用途**：叠加野生型与突变体（或两个同源蛋白）结构，比较构象差异。

1. 分别载入 WT 和 Mut 两个结构文件
2. 点击「自动 Cα 叠加」
   - **同源蛋白残基编号对不上时**，勾选「序列比对辅助」，用序列比对自动建立对应关系
3. 查看指标：Cα RMSD、突变位点数、序列一致性、比对残基数
4. 对比方式：
   - **并排显示**：WT | Mut 左右分屏，鼠标操作**同步旋转/缩放**，方便对照
   - 突变位点标签、位移虚线、分子表面与透明度均可调（开启表面时卡通自动不透明，防止穿模斑块）

---

## 模块三：差异比对（突变前后互作变化）

**用途**：比较 WT-Partner 与 Mut-Partner 两个复合物，找出突变导致**丢失或新增**的互作。

1. 分别载入 WT-Partner 复合物、Mut-Partner 复合物
2. 「链角色设定」：标记各链角色（绿=WT 蛋白，红=Mut 蛋白，紫=Partner），可点「自动分配」，也可用下拉框逐链直选
3. 点击「自动以 Partner 链叠加」→ 执行互作差异分析
4. 查看结果：WT 特有（丢失）/ 两者共有 / Mut 特有（获得）的互作清单，及两组互作统计对比
5. 导出：差异报告（CSV）、截图（PNG）

---

## 常见问题

| 问题 | 建议 |
|---|---|
| 页面卡顿 | 大结构先关闭「显示全部残基标签」和分子表面；必要时用界面聚焦代替完整表面 |
| 叠加对不齐 | 同源蛋白务必勾选「序列比对辅助」 |
| 互作位点点了没反应 | 确认已在列表中点击记录本身（而非统计数字） |
| AF3 结果怎么看 | pLDDT 过滤自动生效，结论中优先看「有效界面残基」和氢键/盐桥等特异性互作 |

---

*版本：2026-09 · 单文件网页应用（3Dmol.js），可离线打开 index.html 使用*
